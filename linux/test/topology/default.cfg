#----------------------------
# SXP configuration file
#---------------------------
#

#default topology configuration file

global = {
    log_level = "alert";
    retry_timer = 5;
    reconciliation_timer = 800;
    speaker_min_hold_time = 900;
    listener_min_hold_time = 800;
    listener_max_hold_time = 1200;
    #keepalive_timer = 600;
    subnet_expansion_limit = 50;
    #default_connection_passwd = "password";
    bind_ip = "127.0.0.1";
    port_number = 64000;
    node_id = 0x00112233;
    enabled = false;
};

peers = (
);

bindings = (
);

### eof
