#----------------------------
# SXP configuration file
#---------------------------
#

#default c+java topology testing configuration file

global = {
    log_level = "alert";
    retry_timer = 5;
    reconciliation_timer = 80;
    speaker_min_hold_time = 90;
    listener_min_hold_time = 90;
    listener_max_hold_time = 120;
    #keepalive_timer = 60;
    subnet_expansion_limit = 50;
    #default_connection_passwd = "password";
    bind_ip = "127.0.0.1";
    port_number = 64999;
    node_id = 0x00112233;
    enabled = false;
};

peers = (
);

bindings = (
);

### eof
